<%= form_for(current_sample_user.relationships.find_by_followed_id(@sample_user),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>